1. Why is Python the most popular language for AI, despite being slow?
*a) It acts as a "glue" language, triggering highly optimized C/C++/CUDA kernels under the hood.
b) It is the fastest interpreted language ever created.
c) It has the best syntax highlighting in IDEs.
d) It was specifically designed for AI and nothing else.

2. What happens when you run a command in NumPy or PyTorch?
*a) You write simple Python, but it triggers optimized C/C++/CUDA code on the CPU or GPU.
b) Python interprets each line one at a time without any optimization.
c) The code is sent to a remote server for execution.
d) Python compiles the code into machine language directly.

3. What is the purpose of a Virtual Environment (venv or conda)?
*a) To create a self-contained directory with a specific Python version and set of libraries to avoid dependency conflicts.
b) To simulate a different operating system.
c) To run Python code in a web browser.
d) To encrypt all Python source code.

4. What problem do Virtual Environments solve?
*a) Dependency Conflicts — where different projects require different versions of the same library.
b) Slow internet connections.
c) Hardware incompatibilities between AMD and Intel CPUs.
d) The inability to use Python on mobile devices.

5. Which command creates a virtual environment using the standard Python method?
*a) python -m venv my_env
b) pip install venv my_env
c) python --create-env my_env
d) conda make my_env

6. Which command activates a conda environment?
*a) conda activate my_env
b) conda start my_env
c) conda run my_env
d) conda open my_env

7. What is Google Colab?
*a) A free, browser-based Jupyter Notebook environment with Python, NumPy, and Pandas pre-installed.
b) A paid cloud database service.
c) A code editor that only works on Chrome OS.
d) A Python compiler for mobile phones.

8. What is a disadvantage of Google Colab?
*a) Sessions time out after inactivity and it is difficult to use with local files.
b) It does not support Python.
c) It only works on Windows.
d) It costs $100 per month.

9. What is a "List Comprehension" in Python?
*a) A concise one-line syntax for generating lists without explicit loops.
b) A way to compress lists into ZIP files.
c) A method for sorting lists alphabetically.
d) A technique for deleting list elements.

10. Why are List Comprehensions often faster than traditional for-loops?
*a) The iteration logic is handled in C rather than Python byte-code.
b) They use parallel GPU processing.
c) They skip every other element.
d) They only work on small lists.

11. What is "Continuous Data"?
*a) Measurements with infinite precision, like Temperature (23.5°C) or Height (1.75m).
b) Data that is always increasing.
c) Data stored in a continuous block of memory.
d) Data that never has missing values.

12. What is "Discrete Data"?
*a) Whole-number counts that cannot be subdivided, like "3 Children" or "5 Packets".
b) Data that is kept private.
c) Data that changes every second.
d) Data stored in separate files.

13. What is "Nominal Data"?
*a) Categories with no inherent order, such as colors (Red, Blue, Green).
b) Data that is named after people.
c) Numerical data with decimal points.
d) Data sorted in ascending order.

14. What is "Ordinal Data"?
*a) Categories with a strict rank, such as T-Shirt sizes (Small < Medium < Large).
b) Data arranged by date.
c) Any data stored in an ordered list.
d) Random data with no structure.

15. What does the principle "Garbage In, Garbage Out" (GIGO) mean?
*a) The quality of your output is determined by the quality of your input data, not your algorithm's sophistication.
b) You should always delete bad code.
c) AI models automatically clean dirty data.
d) Only use premium hardware for AI.

16. What are the three common data quality issues mentioned in the notes?
*a) Missing Data, Outliers, and Normalization problems.
b) Slow internet, low RAM, and small screens.
c) Bad formatting, wrong fonts, and color issues.
d) Incorrect file names, missing folders, and broken links.

17. What does "Impute" mean in the context of missing data?
*a) Fill in missing values with a calculated replacement (e.g., mean or median).
b) Delete all rows with missing values.
c) Ignore missing values entirely.
d) Convert missing values to zero.

18. Why is Normalization important for AI models?
*a) Features on different scales (e.g., Age 0-100 vs Salary 0-1,000,000) must be scaled to the same range so the model treats them equally.
b) It makes the data look prettier in charts.
c) It converts all data to the same data type.
d) It removes duplicate records.

19. What is a NumPy ndarray?
*a) A dense, fixed-type array stored sequentially in memory for fast computation.
b) A Python dictionary with numerical keys.
c) A linked list of floating-point numbers.
d) A text file containing comma-separated values.

20. Why are NumPy arrays faster than Python lists?
*a) They are C-Contiguous in memory, enabling SIMD (Single Instruction, Multiple Data) vectorization.
b) They use more RAM.
c) They are interpreted by a faster version of Python.
d) They automatically run on GPUs.

21. What does "C-Contiguous" mean for NumPy arrays?
*a) The array elements are stored sequentially (next to each other) in RAM.
b) The array is written in the C programming language.
c) The array can only store characters.
d) The array is compressed using a C algorithm.

22. What does SIMD stand for?
*a) Single Instruction, Multiple Data.
b) Simple Input, Multiple Display.
c) Sorted Index, Multi-Dimensional.
d) Standard Interface, Modular Design.

23. How much faster can NumPy operations be compared to pure Python loops?
*a) 50x to 100x faster.
b) About 2x faster.
c) Exactly the same speed.
d) 1000x faster in all cases.

24. What is "Broadcasting" in NumPy?
*a) Performing arithmetic on arrays of different shapes by stretching the smaller array to match.
b) Sending arrays to multiple computers simultaneously.
c) Converting arrays to a different data type.
d) Printing arrays to the console.

25. What is the first rule of Broadcasting?
*a) If dimensions differ, left-pad the smaller shape with 1s.
b) Always convert arrays to the same data type first.
c) Both arrays must have the same number of elements.
d) Broadcasting only works with 2D arrays.

26. When does Broadcasting raise an error?
*a) When dimensions don't match and neither is 1.
b) When both arrays have the same shape.
c) When one array has more elements than the other.
d) When the data type is float64.

27. In the Broadcasting example, adding a (3,) vector to a (100, 3) matrix works because:
*a) NumPy implicitly stretches the vector to (100, 3) by repeating it 100 times.
b) NumPy truncates the matrix to (1, 3).
c) NumPy throws an error.
d) NumPy converts the vector to a string.

28. What does np.zeros((3, 3)) create?
*a) A 3x3 matrix of pure zeros.
b) A 3x3 matrix of ones.
c) A 3x3 identity matrix.
d) A 3x3 matrix of random numbers.

29. What is np.arange(0, 20, 2) used for?
*a) Generating a sequence of even numbers from 0 to 18.
b) Creating an array of 20 zeros.
c) Generating 20 random numbers.
d) Creating a 20x2 matrix.

30. When should you use np.linspace instead of np.arange?
*a) When you need perfectly evenly-spaced points for plotting smooth functions.
b) When you need integer steps.
c) When you need random numbers.
d) When you are working with strings.

31. What does the .shape attribute of a NumPy array tell you?
*a) A tuple representing the dimensions (e.g., (100, 3) means 100 rows, 3 columns).
b) The data type of the array.
c) The memory address of the array.
d) The total number of characters in the array.

32. Why is the .shape attribute critical in Deep Learning?
*a) Debugging is 90% checking if matrix shapes match for operations.
b) It determines the color of plots.
c) It controls the learning rate.
d) It sets the number of training epochs.

33. What does the .dtype attribute reveal about a NumPy array?
*a) The data type of elements (e.g., float32, int64, uint8).
b) The physical location of the array in memory.
c) The number of dimensions.
d) The total size in bytes.

34. What does .ndim tell you about a NumPy array?
*a) The rank (number of dimensions): 1 for vector, 2 for matrix, 3 for volume.
b) The number of elements.
c) The data type.
d) The memory usage.

35. In NumPy, arr[0, 5] accesses:
*a) The element at row 0, column 5.
b) The sum of elements 0 and 5.
c) A slice of elements from 0 to 5.
d) The 5th row, 0th column.

36. What is special about NumPy slicing compared to copying?
*a) Slicing returns a View (not a copy), so changes to the slice affect the original array.
b) Slicing always creates a new copy of the data.
c) Slicing is slower than copying.
d) Slicing only works on 1D arrays.

37. What is a Boolean Mask in NumPy?
*a) A True/False array that selects data based on conditions, like a SQL WHERE clause.
b) A way to encrypt array values.
c) A method for sorting arrays.
d) A technique for compressing arrays.

38. The expression data[data > 5] is an example of:
*a) Boolean Masking (filtering elements greater than 5).
b) Matrix multiplication.
c) Array concatenation.
d) Broadcasting.

39. What does the code data[data < 0] = 0 do?
*a) Replaces all negative values with zero (ReLU-like functionality).
b) Deletes all elements less than zero.
c) Sets all elements to zero.
d) Counts the number of negative values.

40. What is the difference between element-wise operations and matrix multiplication?
*a) Element-wise operates on corresponding cells (A * B); matrix multiplication does row-times-column dot products (A @ B).
b) They are identical operations.
c) Element-wise is faster than matrix multiplication.
d) Matrix multiplication only works on 1D arrays.

41. What does arr.mean(axis=0) compute?
*a) The average for each column (collapsing the rows).
b) The average of the entire array.
c) The average for each row.
d) The median for each column.

42. What does arr.max(axis=1) compute?
*a) The maximum value for each row (collapsing the columns).
b) The maximum of the entire array.
c) The maximum for each column.
d) The minimum for each row.

43. What does arr.reshape(3, 4) do?
*a) Re-interprets the same raw data as a 3-row, 4-column matrix without moving memory.
b) Creates a new array with random values in a 3x4 shape.
c) Deletes elements to fit a 3x4 shape.
d) Duplicates the array 3 times.

44. What does arr.flatten() do?
*a) Converts a multi-dimensional array into a 1D vector.
b) Sorts all elements in ascending order.
c) Removes all zero elements.
d) Transposes the array.

45. What does arr.T do?
*a) Transposes the array (swaps rows and columns).
b) Trims the array to half its size.
c) Converts the array to text.
d) Triples every element.

46. Why is flatten() important in Deep Learning?
*a) It transitions from a 2D image (processed by CNN) to a 1D vector (fed into a classifier).
b) It removes duplicate values from an array.
c) It speeds up training by reducing dimensions.
d) It converts pixel values to strings.

47. What does Pandas add on top of NumPy?
*a) Labels — allowing data access by meaningful names (e.g., "Age", "Salary") instead of indices.
b) GPU acceleration.
c) 3D visualization.
d) Audio processing capabilities.

48. What is the difference between df.loc and df.iloc in Pandas?
*a) loc uses label-based access; iloc uses integer position-based access.
b) loc is faster; iloc is slower.
c) loc works only on rows; iloc works only on columns.
d) They are identical.

49. What is a key feature of df.loc slicing?
*a) It includes the last element in slices (unlike standard Python slicing).
b) It excludes the first element.
c) It only works with numerical indices.
d) It returns a copy, not a view.

50. What does df.iloc follow for slicing conventions?
*a) Standard Python slicing (exclusive of the last element).
b) MATLAB-style slicing (inclusive of the last element).
c) SQL-style filtering.
d) Random access patterns.

51. What is the "Split-Apply-Combine" strategy in Pandas?
*a) Split data into groups by a key, apply an aggregation function to each group, combine results into a summary table.
b) Split a file into multiple CSVs, apply formatting, combine them again.
c) Split code into functions, apply them to data, combine outputs.
d) Split a dataset in half, train on one, test on the other.

52. In Pandas, df.groupby('Dept')['Salary'].mean() performs:
*a) Split-Apply-Combine: groups by Department, calculates mean Salary per department.
b) Sorts the DataFrame by Department and Salary.
c) Deletes all rows where Salary is below the mean.
d) Creates a new column called "mean".

53. What does the .agg(['min', 'max']) method do after a groupby?
*a) Computes multiple statistics (minimum and maximum) at once for each group.
b) Aggregates all groups into a single value.
c) Filters out groups that are too small.
d) Sorts groups alphabetically.

54. What is an "Inner Join" when merging DataFrames?
*a) The intersection — only keeps rows where keys exist in BOTH tables.
b) Keeps all rows from both tables.
c) Keeps all rows from the left table only.
d) Deletes all matching rows.

55. What is a "Left Join" when merging DataFrames?
*a) Keeps all rows from the Left table; missing data in the Right table becomes NaN.
b) Keeps all rows from the Right table.
c) Only keeps matching rows.
d) Deletes the left table after merging.

56. What is the result of a Left Join when a key exists in the left table but not the right?
*a) The row is kept, and the missing right-table values are filled with NaN.
b) The row is deleted.
c) An error is thrown.
d) The row is duplicated.

57. What is the syntax for merging two DataFrames in Pandas?
*a) pd.merge(left, right, on='key', how='inner')
b) pd.join(left, right, key='id')
c) pd.concat(left, right, axis='key')
d) pd.combine(left, right, on='key')

58. In Matplotlib, what is a "Figure"?
*a) The blank canvas or window on which plots are drawn.
b) A single data point on a graph.
c) A styling configuration for plots.
d) A mathematical function.

59. In Matplotlib, what is an "Axes"?
*a) The actual plot/graph drawn on the canvas; a Figure can have multiple Axes (subplots).
b) The x-axis and y-axis labels only.
c) The legend of the plot.
d) The title of the plot.

60. What is the correct Object-Oriented way to create a Matplotlib plot?
*a) fig, ax = plt.subplots(figsize=(10, 6))
b) plt.plot(x, y)
c) plot = new Plot(x, y)
d) fig = plt.figure(); fig.plot(x, y)

61. What does Seaborn's Heatmap visualize?
*a) The Correlation Matrix (df.corr()), showing which features are related.
b) The geographical distribution of data.
c) The temperature of the CPU during computation.
d) The amount of RAM being used.

62. What does a Seaborn Pairplot create?
*a) A grid of scatterplots comparing every variable against every other variable.
b) A single bar chart of all variables.
c) A 3D surface plot.
d) A pie chart of variable proportions.

63. In the Network Traffic Case Study, what does the dataset contain?
*a) Network flow statistics labeled as either "benign" or "suspicious" (potential attack).
b) Customer purchase histories.
c) Weather temperature readings.
d) Student exam scores.

64. What is the first step when analyzing a new dataset?
*a) Loading it and checking its health (dimensions, missing values, class balance).
b) Immediately training a neural network.
c) Deleting all rows with any missing values.
d) Converting all values to strings.

65. Which Pandas method checks for missing values (Non-Null counts)?
*a) df.info()
b) df.describe()
c) df.head()
d) df.columns

66. What does df['label'].value_counts() tell you?
*a) The class balance — how many rows belong to each label category.
b) The total number of columns.
c) The data types of each column.
d) The mean value of each column.

67. What is a robust way to fill missing numerical data?
*a) Use the median, which is robust to outliers.
b) Always use zero.
c) Delete the entire column.
d) Use the maximum value.

68. In the case study, what cleaning step is performed on missing 'protocol' values?
*a) Rows with missing protocol values are dropped (df.dropna(subset=['protocol'])).
b) Missing protocols are filled with "unknown".
c) The entire protocol column is deleted.
d) Missing protocols are replaced with the most common protocol.

69. What question does the analysis phase of the case study try to answer?
*a) "Do attacks send more data (bytes) than normal traffic?"
b) "Which protocol is most common?"
c) "How many users are online?"
d) "What time of day has the most traffic?"

70. The code df.groupby('label')['byte_count'].mean() calculates:
*a) The average byte count for each traffic label (benign vs suspicious).
b) The total byte count across all labels.
c) The maximum byte count for the most common label.
d) The median byte count for suspicious traffic only.

71. What does the Boolean Mask df[(df['label'] == 'suspicious') & (df['byte_count'] > 1_000_000)] do?
*a) Filters rows that are both "suspicious" AND have byte count greater than 1MB.
b) Deletes all suspicious rows.
c) Converts byte count to megabytes.
d) Sorts the DataFrame by byte count.

72. Why is plt.yscale('log') used in the scatter plot?
*a) Because the values vary wildly, and a log scale reveals patterns at all magnitudes.
b) Because logarithmic scales are always prettier.
c) Because the data is already in logarithmic format.
d) Because linear scales don't work with scatter plots.

73. What did the scatter plot reveal about suspicious network traffic?
*a) Attack traffic consistently transmits significantly more bytes than normal traffic, even for short durations.
b) Attack traffic always has the shortest duration.
c) There is no difference between benign and suspicious traffic.
d) Benign traffic transmits more bytes than attack traffic.

74. What AI-relevant conclusion does the case study draw from the scatter plot?
*a) "Volume" (byte count) is a strong feature for detecting these attacks.
b) "Duration" is the only useful feature.
c) The data cannot be used for machine learning.
d) All traffic should be classified as suspicious.

75. True or False: Python is fast because its interpreter runs natively on the GPU.
False

76. True or False: Virtual Environments solve the problem of Dependency Conflicts between projects.
True

77. True or False: A List Comprehension [x**2 for x in range(10)] is just syntactic sugar with no performance benefit.
False

78. True or False: Continuous Data includes measurements like Temperature and Height stored as float.
True

79. True or False: Nominal Data has a strict ordering (Red > Blue > Green).
False

80. True or False: Ordinal Data preserves a rank order (Small < Medium < Large).
True

81. True or False: The principle "Garbage In, Garbage Out" means that even the best algorithm cannot fix bad data.
True

82. True or False: NumPy's ndarray stores elements of mixed types (integers and strings together).
False

83. True or False: NumPy arrays are typically 50x to 100x faster than pure Python loops.
True

84. True or False: Broadcasting in NumPy duplicates memory to stretch arrays.
False

85. True or False: np.linspace(0, 1, 5000) generates 5000 evenly spaced points between 0 and 1.
True

86. True or False: In NumPy, slicing an array always creates a full copy of the data.
False

87. True or False: arr.mean(axis=0) computes the mean for each row.
False

88. True or False: arr.reshape() moves data in memory to a new location.
False

89. True or False: Pandas loc includes the last element in a slice, unlike standard Python.
True

90. True or False: An Inner Join keeps all rows from both DataFrames.
False

91. True or False: Outliers in data can skew an AI model's predictions.
True

92. True or False: Seaborn is built on top of Matplotlib.
True

93. True or False: A Pairplot is usually the first visualization a Data Scientist runs on a new dataset.
True

94. True or False: df.dropna() drops rows with missing values.
True

95. True or False: The @ operator in NumPy performs element-wise multiplication.
False

96. What does conda create --name my_env python=3.9 do?
*a) Creates a new conda environment called "my_env" with Python version 3.9 installed.
b) Updates the existing environment to Python 3.9.
c) Deletes the environment named "my_env".
d) Creates a Python script named my_env.py.

97. Why is conda recommended over venv for Data Science?
*a) It handles non-Python dependencies (like CUDA for GPU) better.
b) It is faster at executing Python code.
c) It only works with Data Science libraries.
d) It does not require an internet connection.

98. What does img.shape returning (1080, 1920, 3) tell you about the array?
*a) It's an HD image: 1080 rows × 1920 columns × 3 color channels (RGB).
b) It's a 1080-element vector.
c) It contains 1080 images.
d) It has 1920 dimensions.

99. If an image uses float64 dtype instead of uint8, how much more RAM does it consume?
*a) 8 times more (float64 is 8 bytes vs uint8's 1 byte).
b) 2 times more.
c) Exactly the same.
d) 64 times more.

100. What is the "Split" step in Split-Apply-Combine?
*a) Divide the DataFrame into small groups based on a key (e.g., "City" or "Department").
b) Split the data into training and testing sets.
c) Divide the code into modules.
d) Break the data into individual characters.

101. What is the "Apply" step in Split-Apply-Combine?
*a) Compute a single summary number for each group (e.g., Average, Sum).
b) Apply a stylesheet to the output.
c) Run the model prediction.
d) Apply a filter to remove outliers.

102. What is the "Combine" step in Split-Apply-Combine?
*a) Paste the group summaries back together into a new summary table.
b) Combine multiple CSV files into one.
c) Merge two models into one.
d) Concatenate strings from different columns.

103. What does the "Outer Join" keep?
*a) The Union — it keeps everything from both tables.
b) Only matching rows.
c) Only rows from the left table.
d) Only rows from the right table.

104. In the notes, what analogy is used for NumPy vs Pandas?
*a) NumPy is the Engine; Pandas is the Dashboard.
b) NumPy is the Car; Pandas is the Road.
c) NumPy is the Fuel; Pandas is the Engine.
d) NumPy is the Software; Pandas is the Hardware.

105. Why are explicit for-loops considered a "code smell" in Python Data Science?
*a) They are verbose and often slower than vectorized or comprehension-based alternatives.
b) They are forbidden by the Python language spec.
c) They cause memory leaks.
d) They only work on Windows.

106. What feature of the case study scatter plot distinguishes attack clusters?
*a) The suspicious (orange) cluster is positioned higher on the Y-axis (more bytes).
b) The suspicious cluster has shorter durations.
c) The benign cluster has more data points.
d) Both clusters overlap completely.

107. True or False: Google Colab provides free access to GPUs.
True

108. True or False: np.ones((2, 2)) creates a matrix of zeros.
False

109. True or False: Boolean Masking in NumPy works like a SQL WHERE clause.
True

110. True or False: The features explored in the case study (volume, duration) will serve as inputs to classifiers in later weeks.
True

111. True or False: Pandas groupby() creates physical copies of tiny DataFrames internally.
False

112. True or False: np.arange(0, 10, 2) produces [0, 2, 4, 6, 8].
True

113. True or False: An Outer Join only keeps rows that match in both tables.
False

114. True or False: The Object-Oriented Interface in Matplotlib uses fig, ax = plt.subplots().
True

115. What is the main takeaway from the Network Traffic Case Study?
*a) NumPy, Pandas, and Matplotlib together form a pipeline to clean, analyze, and visualize data to discover features useful for AI models.
b) AI models can work without any data preprocessing.
c) Network traffic data is impossible to analyze with Python.
d) Visualization is optional and not part of the analysis loop.

blank 1. Python acts as a "_______" language, triggering optimized C/C++/CUDA kernels under the hood.
a. glue

blank 2. A _______ creates a self-contained directory with its own Python version and libraries to avoid dependency conflicts.
a. Virtual Environment

blank 3. The principle "_______ In, _______ Out" (GIGO) means data quality determines output quality.
a. Garbage, Garbage

blank 4. _______ Data consists of whole-number counts that cannot be meaningfully subdivided.
a. Discrete

blank 5. _______ Data consists of categories with no inherent order, such as colors.
a. Nominal

blank 6. _______ Data consists of categories with a strict rank, such as T-Shirt sizes.
a. Ordinal

blank 7. NumPy arrays are stored _______ in memory (C-Contiguous), enabling fast SIMD operations.
a. sequentially

blank 8. _______ is NumPy's ability to perform arithmetic on arrays of different shapes by stretching the smaller array.
a. Broadcasting

blank 9. The ._______ attribute of a NumPy array returns a tuple representing its dimensions (e.g., rows, columns).
a. shape

blank 10. The ._______ attribute reveals the data type of elements in a NumPy array (e.g., float32, int64).
a. dtype

blank 11. A _______ Mask is a True/False array used to filter data based on conditions, similar to a SQL WHERE clause.
a. Boolean

blank 12. The arr._______() method converts a multi-dimensional array into a 1D vector.
a. flatten

blank 13. Pandas adds _______ to NumPy arrays, allowing data access by meaningful names like "Age" or "Salary".
a. Labels

blank 14. df._______ uses label-based access, while df._______ uses integer position-based access.
a. loc, iloc

blank 15. The _______ strategy in Pandas involves splitting data into groups, applying an aggregation, and combining the results.
a. Split-Apply-Combine

blank 16. An Inner Join keeps only the _______ — rows where keys exist in BOTH tables.
a. intersection

blank 17. In Matplotlib, the _______ is the blank canvas, and the _______ is the actual plot drawn on it.
a. Figure, Axes

blank 18. Seaborn's _______ visualizes the Correlation Matrix, showing which features are related to each other.
a. Heatmap

blank 19. To fill missing numerical values robustly, use the _______, which is resistant to outliers.
a. median

blank 20. In the Network Traffic Case Study, byte _______ was identified as a strong feature for detecting attacks.
a. count


